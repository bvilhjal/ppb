# Agent notes

- Housekeeping: keep the session todo list tidy — clear it (or rewrite it for
  the new task) once the work it tracked is done; don't leave a stale,
  all-completed list lingering between tasks.
