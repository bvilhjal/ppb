[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ppb"
version = "0.0.1.dev2"
description = "Measure polygenic-score accuracy in a target ancestry from summary statistics alone"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "Bjarni J. Vilhjalmsson" },
]
dependencies = [
    "numpy>=1.24",
    "numba>=0.59",
]

[project.optional-dependencies]
test = ["pytest>=7"]
ldref = ["rdata>=1.0"]   # scripts/bigsnpr_ldref_to_ppb.py (reads R serializations)

[project.scripts]
ppb = "ppb.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
# The archived oracle notebook is kept as a historical record, not linted.
extend-exclude = ["archive"]

[tool.ruff.lint]
select = ["F"]
